name: gigme_frontend
description: A Flutter frontend for the GigMe freelancing marketplace.
publish_to: 'none' # This app is not published to pub.dev

# The following defines the version and build number for your application.
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # HTTP client for REST API requests
  http: ^1.1.0

  # Cupertino icons for iOS styling
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true